services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: devinsight_ai_pro
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - devinsight_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U postgres -d devinsight_ai_pro
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  devinsight_postgres_data:
